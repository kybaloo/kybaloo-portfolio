# Plan 2 — Sanity

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre tout le contenu du portfolio éditable depuis un Studio Sanity embarqué sur `/studio`, typé de bout en bout par TypeGen, avec le contenu de l'ancien site importé depuis `docs/content-archive/`.

**Architecture:** Sanity fournit le contenu, Next le rend. Le Studio vit dans son propre groupe de routes avec sa racine HTML, hors du segment `[locale]` qui porte celle du site. L'internationalisation du contenu est **hybride** : au niveau champ pour les documents structurés dont images, liens et dates sont communs aux deux langues ; au niveau document pour les articles, dont les textes divergent réellement. Les requêtes GROQ vivent dans `src/sanity/queries/` et sont la source des types générés.

**Tech Stack:** Sanity 6.12 · next-sanity 13.3 · @sanity/image-url 2.1 · @sanity/vision 6.12 · @sanity/document-internationalization 6.2 · sanity-plugin-internationalized-array 5.2 · @sanity/language-filter 5.0 · styled-components 6.1

**Spec:** `docs/superpowers/specs/2026-09-05-portfolio-refonte-design.md`

**Plan précédent :** `docs/superpowers/plans/2026-09-05-fondations.md` — livré, revu, 31 commits sur `dev`. Son journal de décisions est dans `docs/superpowers/2026-09-05-fondations-journal-decisions.md`.

## Global Constraints

Ces contraintes s'appliquent à **toutes** les tâches de ce plan.

- **Next `16.3.4`, React `19.2.3`, Node `24`** — déjà en place. `next-sanity@13` exige `next: "^16.0.0-0"` et `react: "^19.2.3"`.
- **Aucune dépendance non importée** dans `package.json`. Chaque paquet ajouté doit être utilisé par le code de la tâche qui l'ajoute.
- **`Link` s'importe toujours depuis `@/i18n/navigation`**, jamais depuis `next/link`.
- **Aucune valeur hexadécimale de couleur dans un composant** du site. Rôles sémantiques uniquement : `bg-paper`, `text-ink`, `text-muted`, `border-line`, `text-accent`, `bg-highlight`, `text-highlight-ink`. _(Le Studio Sanity a son propre thème et n'est pas concerné.)_
- **Aucune bibliothèque d'animation** (`framer-motion`, `motion`, `animejs`, `gsap`).
- **`middleware.ts` n'existe pas** — le fichier s'appelle `src/proxy.ts`. Son matcher exclut déjà `api`, `studio`, `admin`, `_next`, `_vercel` et les chemins contenant un point, chacun ancré à une frontière de segment. **Ne pas y toucher.**
- **Le token Sanity est strictement serveur.** Jamais préfixé `NEXT_PUBLIC_`, jamais importé depuis un composant client.
- **`/studio` doit être en `noindex`.**
- **`npm` uniquement**, jamais `pnpm` ni `yarn`. Si `pnpm-lock.yaml` ou `pnpm-workspace.yaml` apparaissent, les supprimer — ce dépôt a déjà subi deux contaminations qui ont cassé Playwright.
- **Locales** : `fr` (défaut) et `en`.
- **Filtre GROQ des champs internationalisés** : `champ[language == "fr"][0].value`. La forme `_key ==` appartient à la v4 du plugin et ne fonctionne plus.
- **La suite doit rester verte** : `npm run format:check`, `validate:archive`, `lint` (0 erreur), `typecheck`, `test`, `build`, `test:e2e`.

## ⚠️ Dépendance humaine bloquante

**La tâche 1 ne peut pas être exécutée par un agent.** Créer un projet Sanity exige un compte et une authentification interactive. Le partenaire humain doit exécuter lui-même la création du projet et fournir les identifiants avant que le reste du plan puisse démarrer. Tout le reste est automatisable.

---

## Structure des fichiers

| Fichier                                        | Responsabilité                                                                   |
| ---------------------------------------------- | -------------------------------------------------------------------------------- |
| `.env.local` (non suivi) · `.env.example`      | Identifiants du projet Sanity                                                    |
| `src/sanity/env.ts`                            | Lecture et validation des variables, échec au démarrage si absentes              |
| `src/sanity/lib/client.ts`                     | Client de lecture, contenu publié                                                |
| `src/sanity/lib/live.ts`                       | `sanityFetch` et `SanityLive` via `defineLive`                                   |
| `src/sanity/lib/image.ts`                      | Constructeur d'URL d'images                                                      |
| `src/sanity/schemas/objects/`                  | Types réutilisables : lien, résultat chiffré                                     |
| `src/sanity/schemas/documents/`                | `profile` · `settings` · `project` · `service` · `experience` · `skill` · `post` |
| `src/sanity/schemas/index.ts`                  | Agrégation des types                                                             |
| `src/sanity/structure.ts`                      | Organisation du Studio, singletons non dupliquables                              |
| `src/sanity/queries/`                          | GROQ — source des types générés                                                  |
| `sanity.config.ts` · `sanity.cli.ts`           | Configuration du Studio et de la CLI                                             |
| `src/app/(studio)/layout.tsx`                  | Racine HTML du Studio, distincte de celle du site                                |
| `src/app/(studio)/studio/[[...tool]]/page.tsx` | Studio embarqué                                                                  |
| `src/app/api/draft-mode/enable/route.ts`       | Activation de la prévisualisation                                                |
| `src/app/api/revalidate/route.ts`              | Webhook Sanity, signature vérifiée                                               |
| `scripts/import-archive.mjs`                   | Import unique du contenu archivé                                                 |
| `sanity.types.ts` (généré)                     | Types issus des requêtes GROQ                                                    |

**Pourquoi un groupe `(studio)` séparé.** `src/app/(site)/[locale]/layout.tsx` porte `<html lang>` par langue. Le Studio n'est pas localisé et ne doit pas hériter du thème ni des polices du site. Next autorise plusieurs racines via les groupes de routes, à condition qu'aucun `src/app/layout.tsx` n'existe — c'est déjà le cas.

---

### Task 1 : Provisionner Sanity et valider l'environnement

**⚠️ Les étapes 1 à 3 exigent le partenaire humain.** Un agent reprend à l'étape 4.

**Files:**

- Create: `.env.example`, `src/sanity/env.ts`, `tests/unit/sanity-env.test.ts`
- Modify: `.gitignore` (vérifier que `.env*.local` y figure)

**Interfaces:**

- Produces: `src/sanity/env.ts` exporte `projectId: string`, `dataset: string`, `apiVersion: string`, `studioUrl: string`, et `readToken(): string` — cette dernière lève si le token manque et n'est appelable que côté serveur. Consommé par les tâches 2, 7 et 8.

- [ ] **Step 1 : (humain) Créer le projet Sanity**

Se connecter et créer le projet :

```bash
npx sanity@latest login
npx sanity@latest projects create "Portfolio Tchangai" --dataset production
```

Noter l'identifiant de projet affiché.

- [ ] **Step 2 : (humain) Créer un token de lecture**

Sur `https://sanity.io/manage`, projet → **API** → **Tokens** → **Add API token**, permission **Viewer**. Copier la valeur : elle n'est affichée qu'une fois.

- [ ] **Step 3 : (humain) Écrire `.env.local`**

```bash
NEXT_PUBLIC_SANITY_PROJECT_ID=<identifiant du projet>
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_STUDIO_URL=http://localhost:3000/studio
SANITY_API_READ_TOKEN=<le token Viewer>
SANITY_REVALIDATE_SECRET=<une chaîne aléatoire, ex: openssl rand -hex 32>
```

Vérifier que `.env.local` n'est **pas** suivi par git : `git check-ignore -v .env.local` doit renvoyer une règle.

- [ ] **Step 4 : Écrire le test qui échoue**

```ts
// tests/unit/sanity-env.test.ts
import {describe, expect, it} from 'vitest';

describe('environnement Sanity', () => {
  it('expose les valeurs publiques nécessaires', async () => {
    const env = await import('@/sanity/env');
    expect(env.projectId).toBeTruthy();
    expect(env.dataset).toBeTruthy();
    expect(env.apiVersion).toMatch(/^\d{4}-\d{2}-\d{2}$/);
  });

  it('refuse un identifiant de projet manquant avec un message explicite', async () => {
    const {assertValue} = await import('@/sanity/env');
    expect(() => assertValue(undefined, 'NEXT_PUBLIC_SANITY_PROJECT_ID manquant')).toThrow(
      /NEXT_PUBLIC_SANITY_PROJECT_ID manquant/,
    );
  });

  it("n'expose jamais le token de lecture sous un nom public", async () => {
    const source = await import('node:fs').then((fs) =>
      fs.readFileSync(new URL('../../src/sanity/env.ts', import.meta.url), 'utf8'),
    );
    expect(source).not.toMatch(/NEXT_PUBLIC_[A-Z_]*TOKEN/);
  });
});
```

- [ ] **Step 5 : Lancer le test pour vérifier qu'il échoue**

Run: `npx vitest run tests/unit/sanity-env.test.ts`
Expected: ÉCHEC — `Cannot find module '@/sanity/env'`.

- [ ] **Step 6 : Écrire `src/sanity/env.ts`**

```ts
/**
 * Variables d'environnement Sanity, validées au chargement du module.
 *
 * Une variable manquante doit faire échouer le démarrage avec un message
 * lisible, pas produire un `undefined` qui se propage jusqu'à une requête
 * silencieusement vide.
 */

export function assertValue<T>(value: T | undefined, errorMessage: string): T {
  if (value === undefined || value === '') {
    throw new Error(errorMessage);
  }
  return value;
}

export const projectId = assertValue(
  process.env.NEXT_PUBLIC_SANITY_PROJECT_ID,
  'Variable manquante : NEXT_PUBLIC_SANITY_PROJECT_ID',
);

export const dataset = assertValue(
  process.env.NEXT_PUBLIC_SANITY_DATASET,
  'Variable manquante : NEXT_PUBLIC_SANITY_DATASET',
);

/** Date figée : la faire évoluer est un choix délibéré, jamais un effet de bord. */
export const apiVersion = '2026-09-01';

export const studioUrl = process.env.NEXT_PUBLIC_SANITY_STUDIO_URL ?? '/studio';

/**
 * Token de lecture, strictement serveur. Ne jamais l'importer depuis un
 * composant client : le préfixe `NEXT_PUBLIC_` est délibérément absent.
 */
export function readToken(): string {
  return assertValue(
    process.env.SANITY_API_READ_TOKEN,
    'Variable manquante : SANITY_API_READ_TOKEN',
  );
}
```

- [ ] **Step 7 : Écrire `.env.example`**

```bash
# Sanity — identifiants du projet de contenu.
# Copier vers .env.local et remplir. .env.local n'est jamais commité.

# Publiques : exposées au navigateur, sans secret.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_STUDIO_URL=http://localhost:3000/studio

# Serveur uniquement. Le token donne accès aux brouillons : ne jamais le
# préfixer NEXT_PUBLIC_, ne jamais l'importer depuis un composant client.
SANITY_API_READ_TOKEN=
SANITY_REVALIDATE_SECRET=

# URL canonique du site. Sur Vercel, VERCEL_PROJECT_PRODUCTION_URL prend le
# relais si celle-ci est absente.
NEXT_PUBLIC_SITE_URL=
```

Si un `.env.example` existe déjà (créé au plan 1 pour `NEXT_PUBLIC_SITE_URL`), le compléter au lieu de l'écraser.

- [ ] **Step 8 : Relancer le test et vérifier la chaîne**

```bash
npx vitest run tests/unit/sanity-env.test.ts
npm run typecheck
npm run format:check
```

Expected: 3 tests PASS, typecheck en 0, formatage conforme.

- [ ] **Step 9 : Commit**

```bash
git add .env.example src/sanity/env.ts tests/unit/sanity-env.test.ts
git commit -m "feat(sanity): Validate Sanity environment variables at startup

A missing variable now fails the boot with a readable message instead of
producing an undefined that propagates into a silently empty query. The
read token deliberately has no NEXT_PUBLIC_ prefix and a test asserts it
never gains one.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2 : Client Sanity et Studio embarqué

**Files:**

- Create: `src/sanity/lib/client.ts`, `src/sanity/lib/live.ts`, `src/sanity/lib/image.ts`, `src/sanity/schemas/index.ts`, `src/sanity/structure.ts`, `sanity.config.ts`, `sanity.cli.ts`, `src/app/(studio)/layout.tsx`, `src/app/(studio)/studio/[[...tool]]/page.tsx`, `tests/e2e/studio.spec.ts`
- Modify: `package.json`

**Interfaces:**

- Consumes: `projectId`, `dataset`, `apiVersion`, `studioUrl`, `readToken()` (tâche 1)
- Produces:
  - `client` (`src/sanity/lib/client.ts`) — client de lecture du contenu publié
  - `sanityFetch` et `SanityLive` (`src/sanity/lib/live.ts`) — consommés par les tâches 6 et suivantes, et par le plan 3
  - `urlFor(source): ImageUrlBuilder` (`src/sanity/lib/image.ts`)
  - `schemaTypes: SchemaTypeDefinition[]` (`src/sanity/schemas/index.ts`) — **tableau vide à cette tâche**, rempli par les tâches 3 à 5
  - `/studio` servi et en `noindex`

- [ ] **Step 1 : Installer les dépendances**

```bash
npm install sanity@^6.12.0 next-sanity@^13.3.4 @sanity/image-url@^2.1.1 @sanity/vision@^6.12.0 styled-components@^6.1.15
```

`styled-components` est un peer requis par le Studio, pas une dépendance morte. `@sanity/vision` ajoute l'onglet d'exploration GROQ dans le Studio — il servira réellement à la tâche 6.

- [ ] **Step 2 : Écrire le test qui échoue**

```ts
// tests/e2e/studio.spec.ts
import {expect, test} from '@playwright/test';

test('le Studio répond et se charge', async ({page}) => {
  const response = await page.goto('/studio');
  expect(response?.status()).toBe(200);
  // Le Studio est une application cliente : on attend son conteneur racine.
  await expect(page.locator('#sanity')).toBeAttached({timeout: 30_000});
});

test('le Studio est en noindex', async ({request}) => {
  const response = await request.get('/studio');
  const robots = response.headers()['x-robots-tag'] ?? '';
  const html = await response.text();
  expect(robots.includes('noindex') || /name="robots"[^>]*noindex/.test(html)).toBe(true);
});

test('le Studio ne porte pas la mise en page du site', async ({page}) => {
  await page.goto('/studio');
  // L'en-tête du site ne doit pas apparaître : le Studio a sa propre racine.
  await expect(page.getByRole('link', {name: 'Florentin Tchangai'})).toHaveCount(0);
});
```

- [ ] **Step 3 : Lancer pour vérifier l'échec**

Run: `npx playwright test tests/e2e/studio.spec.ts`
Expected: ÉCHEC — `/studio` renvoie 404.

- [ ] **Step 4 : Écrire le client et les utilitaires**

```ts
// src/sanity/lib/client.ts
import {createClient} from 'next-sanity';
import {apiVersion, dataset, projectId, studioUrl} from '../env';

export const client = createClient({
  projectId,
  dataset,
  apiVersion,
  // Le CDN sert le contenu publié : plus rapide, suffisant pour le site.
  useCdn: true,
  perspective: 'published',
  stega: {studioUrl},
});
```

```ts
// src/sanity/lib/live.ts
import {defineLive} from 'next-sanity';
import {client} from './client';
import {readToken} from '../env';

const token = readToken();

export const {sanityFetch, SanityLive} = defineLive({
  client,
  serverToken: token,
  browserToken: token,
  // `strict` fait échouer une requête mal formée au lieu de renvoyer null.
  strict: true,
});
```

```ts
// src/sanity/lib/image.ts
import createImageUrlBuilder from '@sanity/image-url';
import type {SanityImageSource} from '@sanity/image-url/lib/types/types';
import {dataset, projectId} from '../env';

const builder = createImageUrlBuilder({projectId, dataset});

export function urlFor(source: SanityImageSource) {
  return builder.image(source);
}
```

- [ ] **Step 5 : Écrire le squelette de schéma et la structure**

```ts
// src/sanity/schemas/index.ts
import type {SchemaTypeDefinition} from 'sanity';

/**
 * Types du Studio. Rempli par les tâches suivantes ; volontairement vide
 * ici pour que le Studio démarre avant qu'aucun schéma n'existe.
 */
export const schemaTypes: SchemaTypeDefinition[] = [];
```

```ts
// src/sanity/structure.ts
import type {StructureResolver} from 'sanity/structure';

/**
 * Les singletons (`profile`, `settings`) n'existent qu'en un exemplaire :
 * on les expose comme documents uniques plutôt que comme listes, sinon un
 * éditeur peut en créer un second et le site ne saura pas lequel servir.
 */
export const SINGLETON_TYPES = new Set(['profile', 'settings']);

export const structure: StructureResolver = (S) =>
  S.list()
    .title('Contenu')
    .items([
      ...S.documentTypeListItems().filter((item) => !SINGLETON_TYPES.has(item.getId() ?? '')),
    ]);
```

- [ ] **Step 6 : Écrire la configuration du Studio**

```ts
// sanity.config.ts
import {visionTool} from '@sanity/vision';
import {defineConfig} from 'sanity';
import {structureTool} from 'sanity/structure';
import {apiVersion, dataset, projectId} from './src/sanity/env';
import {schemaTypes} from './src/sanity/schemas';
import {structure} from './src/sanity/structure';

export default defineConfig({
  name: 'default',
  title: 'Portfolio — Florentin Tchangai',
  basePath: '/studio',
  projectId,
  dataset,
  schema: {types: schemaTypes},
  plugins: [structureTool({structure}), visionTool({defaultApiVersion: apiVersion})],
});
```

```ts
// sanity.cli.ts
import {defineCliConfig} from 'sanity/cli';
import {dataset, projectId} from './src/sanity/env';

export default defineCliConfig({
  api: {projectId, dataset},
  // Les types sont générés depuis les requêtes de src/sanity/queries/.
  autoUpdates: false,
});
```

- [ ] **Step 7 : Écrire la racine et la page du Studio**

```tsx
// src/app/(studio)/layout.tsx
/**
 * Racine HTML du Studio, distincte de celle du site.
 *
 * Le Studio n'est pas localisé et ne doit hériter ni du thème ni des polices
 * du site. Next autorise plusieurs racines via les groupes de routes, à
 * condition qu'aucun src/app/layout.tsx n'existe — c'est le cas ici.
 */
export default function StudioRootLayout({children}: {children: React.ReactNode}) {
  return (
    <html lang="fr">
      <body style={{margin: 0}}>{children}</body>
    </html>
  );
}
```

```tsx
// src/app/(studio)/studio/[[...tool]]/page.tsx
import {NextStudio} from 'next-sanity/studio';
import config from '../../../../../sanity.config';

export {viewport} from 'next-sanity/studio';

export const dynamic = 'force-static';

export const metadata = {
  title: 'Studio — Portfolio',
  robots: {index: false, follow: false},
};

export default function StudioPage() {
  return <NextStudio config={config} />;
}
```

- [ ] **Step 8 : Vérifier**

```bash
npm run lint && npm run typecheck && npm run build
npx playwright test tests/e2e/studio.spec.ts
```

Expected: les trois commandes en 0, les 3 tests du Studio PASS.

Si le build se plaint de la taille du bundle du Studio, c'est attendu : le Studio est une application à part entière, servie uniquement sur `/studio`.

- [ ] **Step 9 : Vérifier que le site n'a pas régressé**

```bash
npm test && npm run test:e2e
```

Expected: 21/21 unitaires (plus les 3 de la tâche 1), 10/10 parcours existants plus les 3 du Studio.

- [ ] **Step 10 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Embed the Studio on /studio with its own HTML root

The Studio is not localised and must not inherit the site's theme or fonts,
so it gets its own route group and root layout. Route groups allow that only
while no src/app/layout.tsx exists, which is already the case.

The read client serves published content over the CDN; defineLive supplies
sanityFetch for the pages that arrive in plan 3.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3 : Objets partagés et singletons

**Files:**

- Create: `src/sanity/schemas/objects/link.ts`, `src/sanity/schemas/objects/outcome.ts`, `src/sanity/schemas/documents/profile.ts`, `src/sanity/schemas/documents/settings.ts`, `tests/unit/sanity-schemas.test.ts`
- Modify: `src/sanity/schemas/index.ts`, `src/sanity/structure.ts`, `sanity.config.ts`, `package.json`

**Interfaces:**

- Consumes: `schemaTypes` (tâche 2)
- Produces:
  - types `link` et `outcome` réutilisables par les tâches 4 et 5
  - documents `profile` et `settings`, singletons
  - le plugin `internationalizedArray` configuré pour `fr`/`en`, réutilisé par les tâches 4 et 5

- [ ] **Step 1 : Installer les plugins d'internationalisation**

```bash
npm install sanity-plugin-internationalized-array@^5.2.4 @sanity/language-filter@^5.0.18
```

`@sanity/language-filter` est un peer **requis** de `sanity-plugin-internationalized-array` ; l'omettre produit un avertissement d'installation et un plugin qui ne se charge pas.

- [ ] **Step 2 : Écrire le test qui échoue**

```ts
// tests/unit/sanity-schemas.test.ts
import {describe, expect, it} from 'vitest';
import {schemaTypes} from '@/sanity/schemas';

const byName = (name: string) => schemaTypes.find((t) => t.name === name);

describe('schémas Sanity', () => {
  it('déclare les singletons profile et settings', () => {
    expect(byName('profile')).toBeDefined();
    expect(byName('settings')).toBeDefined();
  });

  it('déclare les objets réutilisables', () => {
    expect(byName('bullet')).toBeDefined();
    expect(byName('link')).toBeDefined();
    expect(byName('outcome')).toBeDefined();
  });

  it("n'a aucun nom de type en double", () => {
    const names = schemaTypes.map((t) => t.name);
    expect(new Set(names).size).toBe(names.length);
  });

  it('donne un titre à chaque type — le Studio est inutilisable sans', () => {
    for (const type of schemaTypes) {
      expect(type.title, `type sans titre : ${type.name}`).toBeTruthy();
    }
  });
});
```

- [ ] **Step 3 : Lancer pour vérifier l'échec**

Run: `npx vitest run tests/unit/sanity-schemas.test.ts`
Expected: ÉCHEC — `profile` introuvable, `schemaTypes` étant vide.

- [ ] **Step 4 : Écrire les objets réutilisables**

```ts
// src/sanity/schemas/objects/link.ts
import {defineField, defineType} from 'sanity';

export const link = defineType({
  name: 'link',
  title: 'Lien',
  type: 'object',
  fields: [
    defineField({name: 'label', title: 'Libellé', type: 'string'}),
    defineField({
      name: 'url',
      title: 'URL',
      type: 'url',
      validation: (rule) => rule.uri({scheme: ['http', 'https', 'mailto']}),
    }),
  ],
  preview: {select: {title: 'label', subtitle: 'url'}},
});
```

```ts
// src/sanity/schemas/objects/bullet.ts
import {defineField, defineType} from 'sanity';

/**
 * Une puce de texte internationalisée.
 *
 * Sanity n'autorise pas un tableau à contenir directement un autre tableau,
 * or `internationalizedArrayString` EST un tableau. Toute liste de textes
 * traduits doit donc passer par un objet enveloppe comme celui-ci.
 */
export const bullet = defineType({
  name: 'bullet',
  title: 'Puce',
  type: 'object',
  fields: [
    defineField({
      name: 'text',
      title: 'Texte',
      type: 'internationalizedArrayString',
      validation: (rule) => rule.required(),
    }),
  ],
  preview: {
    select: {text: 'text.0.value'},
    prepare: ({text}) => ({title: text ?? '(vide)'}),
  },
});
```

```ts
// src/sanity/schemas/objects/outcome.ts
import {defineField, defineType} from 'sanity';

/**
 * Un résultat mesurable. C'est ce qui distingue une fiche projet d'une
 * liste de fonctionnalités : « 50 M$ de demandes traitées » vaut mieux que
 * « traitement des demandes ».
 */
export const outcome = defineType({
  name: 'outcome',
  title: 'Résultat',
  type: 'object',
  fields: [
    defineField({
      name: 'value',
      title: 'Valeur',
      type: 'string',
      description: 'Le chiffre ou la mesure. Ex. : « 40 % », « 50 M$ », « 12 000 ».',
      validation: (rule) => rule.required(),
    }),
    defineField({
      name: 'label',
      title: 'Ce que la valeur mesure',
      type: 'internationalizedArrayString',
      validation: (rule) => rule.required(),
    }),
  ],
  preview: {
    select: {value: 'value', label: 'label.0.value'},
    prepare: ({value, label}) => ({title: value, subtitle: label}),
  },
});
```

- [ ] **Step 5 : Écrire les singletons**

```ts
// src/sanity/schemas/documents/profile.ts
import {defineField, defineType} from 'sanity';

export const profile = defineType({
  name: 'profile',
  title: 'Profil',
  type: 'document',
  fields: [
    defineField({name: 'name', title: 'Nom', type: 'string', validation: (r) => r.required()}),
    defineField({
      name: 'role',
      title: 'Positionnement',
      type: 'internationalizedArrayString',
      description: 'Ex. : « Architecte technologique ». Pas « développeur full-stack ».',
      validation: (r) => r.required(),
    }),
    defineField({name: 'location', title: 'Lieu', type: 'string'}),
    defineField({
      name: 'bio',
      title: 'Biographie',
      type: 'internationalizedArrayText',
    }),
    defineField({
      name: 'photo',
      title: 'Photo',
      type: 'image',
      options: {hotspot: true},
      fields: [
        defineField({
          name: 'alt',
          title: 'Texte alternatif',
          type: 'internationalizedArrayString',
        }),
      ],
    }),
    defineField({
      name: 'available',
      title: 'Disponible pour des missions',
      type: 'boolean',
      initialValue: true,
    }),
    defineField({name: 'links', title: 'Liens', type: 'array', of: [{type: 'link'}]}),
    defineField({name: 'resumeFr', title: 'CV français (PDF)', type: 'file'}),
    defineField({name: 'resumeEn', title: 'CV anglais (PDF)', type: 'file'}),
  ],
  preview: {select: {title: 'name'}, prepare: ({title}) => ({title: title ?? 'Profil'})},
});
```

```ts
// src/sanity/schemas/documents/settings.ts
import {defineField, defineType} from 'sanity';

export const settings = defineType({
  name: 'settings',
  title: 'Réglages du site',
  type: 'document',
  fields: [
    defineField({
      name: 'title',
      title: 'Titre par défaut',
      type: 'internationalizedArrayString',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'description',
      title: 'Description par défaut',
      type: 'internationalizedArrayText',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'ogImage',
      title: 'Image de partage par défaut',
      type: 'image',
      options: {hotspot: true},
    }),
  ],
  preview: {prepare: () => ({title: 'Réglages du site'})},
});
```

- [ ] **Step 6 : Agréger les types et configurer le plugin**

```ts
// src/sanity/schemas/index.ts
import type {SchemaTypeDefinition} from 'sanity';
import {bullet} from './objects/bullet';
import {link} from './objects/link';
import {outcome} from './objects/outcome';
import {profile} from './documents/profile';
import {settings} from './documents/settings';

export const schemaTypes: SchemaTypeDefinition[] = [
  // Objets réutilisables
  bullet,
  link,
  outcome,
  // Singletons
  profile,
  settings,
];
```

Dans `sanity.config.ts`, ajouter le plugin d'internationalisation au niveau champ :

```ts
import {internationalizedArray} from 'sanity-plugin-internationalized-array';

// … dans plugins: [ … ]
internationalizedArray({
  languages: [
    {id: 'fr', title: 'Français'},
    {id: 'en', title: 'English'},
  ],
  defaultLanguages: ['fr'],
  fieldTypes: ['string', 'text'],
}),
```

Ce plugin génère les types `internationalizedArrayString` et `internationalizedArrayText` utilisés ci-dessus.

- [ ] **Step 7 : Empêcher la duplication des singletons**

Dans `src/sanity/structure.ts`, remplacer la liste par :

```ts
import type {StructureResolver} from 'sanity/structure';

export const SINGLETON_TYPES = new Set(['profile', 'settings']);

export const structure: StructureResolver = (S) =>
  S.list()
    .title('Contenu')
    .items([
      S.listItem()
        .title('Profil')
        .id('profile')
        .child(S.document().schemaType('profile').documentId('profile')),
      S.listItem()
        .title('Réglages du site')
        .id('settings')
        .child(S.document().schemaType('settings').documentId('settings')),
      S.divider(),
      ...S.documentTypeListItems().filter((item) => !SINGLETON_TYPES.has(item.getId() ?? '')),
    ]);
```

Et dans `sanity.config.ts`, interdire la création et la suppression de ces documents :

```ts
document: {
  actions: (input, context) =>
    SINGLETON_TYPES.has(context.schemaType)
      ? input.filter(({action}) => action !== 'unpublish' && action !== 'duplicate' && action !== 'delete')
      : input,
},
```

en important `SINGLETON_TYPES` depuis `./src/sanity/structure`.

- [ ] **Step 8 : Vérifier**

```bash
npx vitest run tests/unit/sanity-schemas.test.ts
npm run lint && npm run typecheck && npm run build
npx playwright test tests/e2e/studio.spec.ts
```

Expected: 4 tests unitaires PASS, les trois commandes en 0, les 3 tests du Studio toujours verts.

- [ ] **Step 9 : Vérifier à l'œil dans le Studio**

```bash
npm run dev
```

Ouvrir `http://localhost:3000/studio`. Vérifier :

- « Profil » et « Réglages du site » apparaissent comme documents uniques, pas comme listes ;
- ouvrir « Profil » : le champ « Positionnement » propose bien deux onglets de langue, français et anglais ;
- aucun bouton ne permet de dupliquer ou de supprimer ces deux documents.

Rapporter ce qui est constaté. Arrêter le serveur.

- [ ] **Step 10 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Add shared objects and the profile and settings singletons

Singletons are exposed as single documents with duplicate and delete removed:
a second profile document would leave the site with no way to know which one
to serve.

Field-level i18n arrives with the internationalized-array plugin, whose
language-filter peer is required rather than optional.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4 : Documents structurés — projet, service, expérience, compétence

**Files:**

- Create: `src/sanity/schemas/documents/project.ts`, `service.ts`, `experience.ts`, `skill.ts`
- Modify: `src/sanity/schemas/index.ts`, `tests/unit/sanity-schemas.test.ts`

**Interfaces:**

- Consumes: types `link` et `outcome` (tâche 3), `internationalizedArrayString` / `internationalizedArrayText` (tâche 3)
- Produces: documents `project`, `service`, `experience`, `skill`. `skill.projects` référence `project` — consommé par les requêtes de la tâche 6 et l'import de la tâche 7.

Ces quatre documents partagent le même modèle : internationalisation **au niveau champ**, car images, stack, liens et dates sont communs aux deux langues. Les dupliquer par langue obligerait à re-téléverser chaque capture d'écran.

- [ ] **Step 1 : Compléter le test**

Ajouter à `tests/unit/sanity-schemas.test.ts` :

```ts
describe('documents structurés', () => {
  it('déclare les quatre types de contenu', () => {
    for (const name of ['project', 'service', 'experience', 'skill']) {
      expect(byName(name), `type manquant : ${name}`).toBeDefined();
    }
  });

  it('donne au projet les champs d’architecture qui portent le positionnement', () => {
    const project = byName('project') as {fields: Array<{name: string}>};
    const names = project.fields.map((f) => f.name);
    for (const field of ['context', 'constraint', 'decisions', 'outcomes']) {
      expect(names, `champ d’architecture manquant : ${field}`).toContain(field);
    }
  });

  it('relie chaque compétence aux projets qui la prouvent', () => {
    const skill = byName('skill') as {fields: Array<{name: string; type: string}>};
    const projects = skill.fields.find((f) => f.name === 'projects');
    expect(projects?.type).toBe('array');
  });

  it('ordonne les services sur l’arc Understand → Improve', () => {
    const service = byName('service') as {
      fields: Array<{name: string; options?: {list?: Array<{value: string}>}}>;
    };
    const stage = service.fields.find((f) => f.name === 'stage');
    expect(stage?.options?.list?.map((o) => o.value)).toEqual([
      'understand',
      'architect',
      'build',
      'measure',
      'improve',
    ]);
  });
});
```

- [ ] **Step 2 : Lancer pour vérifier l'échec**

Run: `npx vitest run tests/unit/sanity-schemas.test.ts`
Expected: ÉCHEC — `type manquant : project`.

- [ ] **Step 3 : Écrire le schéma de projet**

```ts
// src/sanity/schemas/documents/project.ts
import {defineField, defineType} from 'sanity';

export const project = defineType({
  name: 'project',
  title: 'Projet',
  type: 'document',
  fields: [
    defineField({
      name: 'title',
      title: 'Titre',
      type: 'string',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'slug',
      title: 'Identifiant d’URL',
      type: 'slug',
      options: {source: 'title', maxLength: 96},
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'summary',
      title: 'Résumé',
      type: 'internationalizedArrayText',
      description: 'Deux lignes maximum. Apparaît dans la liste des travaux.',
    }),
    defineField({name: 'year', title: 'Année', type: 'number'}),
    defineField({name: 'client', title: 'Client ou contexte', type: 'string'}),
    defineField({name: 'role', title: 'Rôle tenu', type: 'internationalizedArrayString'}),
    defineField({
      name: 'featured',
      title: 'Mis en avant sur l’accueil',
      type: 'boolean',
      initialValue: false,
    }),
    defineField({
      name: 'order',
      title: 'Ordre d’affichage',
      type: 'number',
      description: 'Plus petit en premier.',
    }),
    defineField({
      name: 'stack',
      title: 'Technologies',
      type: 'array',
      of: [{type: 'string'}],
      options: {layout: 'tags'},
    }),
    defineField({
      name: 'images',
      title: 'Captures',
      type: 'array',
      of: [
        {
          type: 'image',
          options: {hotspot: true},
          fields: [
            defineField({
              name: 'alt',
              title: 'Texte alternatif',
              type: 'internationalizedArrayString',
            }),
          ],
        },
      ],
    }),
    defineField({name: 'links', title: 'Liens', type: 'array', of: [{type: 'link'}]}),

    // ---- Champs d'architecture ----
    // Sans eux, une fiche prouve qu'on sait développer, pas qu'on sait
    // concevoir. Ils sont optionnels : une fiche incomplète affiche moins.
    defineField({
      name: 'context',
      title: 'Contexte',
      type: 'internationalizedArrayText',
      description: 'Deux lignes : à quel besoin ce projet répondait-il ?',
      group: 'architecture',
    }),
    defineField({
      name: 'constraint',
      title: 'Contrainte principale',
      type: 'internationalizedArrayString',
      description: 'Une ligne. Ex. : « 40 % des sites sans réseau stable ».',
      group: 'architecture',
    }),
    defineField({
      name: 'decisions',
      title: 'Décisions d’architecture',
      type: 'array',
      // `bullet` et non `internationalizedArrayString` : Sanity refuse un
      // tableau contenant directement un autre tableau.
      of: [{type: 'bullet'}],
      description: 'Trois à cinq puces courtes. Ce que vous avez arbitré, et contre quoi.',
      group: 'architecture',
    }),
    defineField({
      name: 'outcomes',
      title: 'Résultats mesurables',
      type: 'array',
      of: [{type: 'outcome'}],
      group: 'architecture',
    }),
  ],
  groups: [{name: 'architecture', title: 'Architecture'}],
  orderings: [
    {
      title: 'Ordre d’affichage',
      name: 'displayOrder',
      by: [{field: 'order', direction: 'asc'}],
    },
  ],
  preview: {
    select: {title: 'title', year: 'year', media: 'images.0'},
    prepare: ({title, year, media}) => ({title, subtitle: year ? String(year) : undefined, media}),
  },
});
```

- [ ] **Step 4 : Écrire les schémas de service, expérience et compétence**

```ts
// src/sanity/schemas/documents/service.ts
import {defineField, defineType} from 'sanity';

/**
 * Les cinq services sont les étapes d'une même expertise, dans l'ordre de
 * l'arc Understand → Architect → Build → Measure → Improve. La numérotation
 * suit cet ordre : un « 01 » en face de la deuxième étape casserait la
 * lecture linéaire que la section doit produire.
 */
export const service = defineType({
  name: 'service',
  title: 'Service',
  type: 'document',
  fields: [
    defineField({
      name: 'number',
      title: 'Numéro',
      type: 'number',
      validation: (r) => r.required().min(1).max(5),
    }),
    defineField({
      name: 'stage',
      title: 'Étape de l’arc',
      type: 'string',
      options: {
        list: [
          {title: 'Understand', value: 'understand'},
          {title: 'Architect', value: 'architect'},
          {title: 'Build', value: 'build'},
          {title: 'Measure', value: 'measure'},
          {title: 'Improve', value: 'improve'},
        ],
      },
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'title',
      title: 'Titre',
      type: 'string',
      description: 'En anglais. Ex. : « Architecture & Systems ».',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'description',
      title: 'Description',
      type: 'internationalizedArrayText',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'keywords',
      title: 'Mots-clés',
      type: 'array',
      of: [{type: 'string'}],
      options: {layout: 'tags'},
    }),
  ],
  orderings: [{title: 'Ordre de l’arc', name: 'arc', by: [{field: 'number', direction: 'asc'}]}],
  preview: {
    select: {title: 'title', number: 'number', stage: 'stage'},
    prepare: ({title, number, stage}) => ({
      title: `${String(number).padStart(2, '0')} — ${title}`,
      subtitle: stage,
    }),
  },
});
```

```ts
// src/sanity/schemas/documents/experience.ts
import {defineField, defineType} from 'sanity';

export const experience = defineType({
  name: 'experience',
  title: 'Expérience',
  type: 'document',
  fields: [
    defineField({
      name: 'position',
      title: 'Poste',
      type: 'internationalizedArrayString',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'organisation',
      title: 'Organisation',
      type: 'string',
      validation: (r) => r.required(),
    }),
    defineField({name: 'location', title: 'Lieu', type: 'string'}),
    defineField({
      name: 'startDate',
      title: 'Début',
      type: 'date',
      options: {dateFormat: 'YYYY-MM'},
    }),
    defineField({
      name: 'endDate',
      title: 'Fin',
      type: 'date',
      options: {dateFormat: 'YYYY-MM'},
      description: 'Laisser vide si le poste est en cours.',
    }),
    defineField({name: 'description', title: 'Description', type: 'internationalizedArrayText'}),
    defineField({
      name: 'achievements',
      title: 'Réalisations',
      type: 'array',
      of: [{type: 'bullet'}],
    }),
    defineField({
      name: 'technologies',
      title: 'Technologies',
      type: 'array',
      of: [{type: 'string'}],
      options: {layout: 'tags'},
    }),
  ],
  orderings: [
    {title: 'Plus récent d’abord', name: 'recent', by: [{field: 'startDate', direction: 'desc'}]},
  ],
  preview: {
    select: {title: 'position.0.value', subtitle: 'organisation'},
  },
});
```

```ts
// src/sanity/schemas/documents/skill.ts
import {defineField, defineType} from 'sanity';

/**
 * Pas de niveau en pourcentage : « JavaScript 95 % » n'est mesuré par rien
 * et signale un portfolio junior au public technique visé. Le niveau réel
 * est prouvé par les projets référencés.
 */
export const skill = defineType({
  name: 'skill',
  title: 'Compétence',
  type: 'document',
  fields: [
    defineField({name: 'name', title: 'Nom', type: 'string', validation: (r) => r.required()}),
    defineField({
      name: 'category',
      title: 'Domaine',
      type: 'string',
      options: {
        list: [
          {title: 'Frontend', value: 'frontend'},
          {title: 'Backend', value: 'backend'},
          {title: 'Données & BI', value: 'data'},
          {title: 'Cloud & DevOps', value: 'cloud'},
          {title: 'Outils', value: 'tools'},
        ],
      },
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'usage',
      title: 'Fréquence d’usage',
      type: 'string',
      options: {
        list: [
          {title: 'Au quotidien', value: 'daily'},
          {title: 'Régulièrement', value: 'regular'},
          {title: 'Notions', value: 'familiar'},
        ],
      },
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'projects',
      title: 'Projets qui la prouvent',
      type: 'array',
      of: [{type: 'reference', to: [{type: 'project'}]}],
      description: 'La preuve plutôt que la déclaration.',
    }),
  ],
  preview: {select: {title: 'name', subtitle: 'usage'}},
});
```

- [ ] **Step 5 : Agréger**

Dans `src/sanity/schemas/index.ts`, importer les quatre nouveaux types et les ajouter au tableau, après les singletons.

- [ ] **Step 6 : Vérifier**

```bash
npx vitest run tests/unit/sanity-schemas.test.ts
npm run lint && npm run typecheck && npm run build
npx playwright test tests/e2e/studio.spec.ts
```

Expected: 8 tests unitaires PASS, les trois commandes en 0, Studio toujours vert.

- [ ] **Step 7 : Vérifier à l'œil**

Lancer `npm run dev`, ouvrir `/studio`, créer un projet de test et confirmer :

- l'onglet « Architecture » regroupe bien contexte, contrainte, décisions et résultats ;
- le champ « Résumé » a deux onglets de langue ;
- dans une compétence, le champ « Projets qui la prouvent » propose bien le projet créé.

Supprimer le projet de test. Rapporter ce qui est constaté. Arrêter le serveur.

- [ ] **Step 8 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Add project, service, experience and skill documents

Projects carry architecture fields — context, constraint, decisions,
outcomes — because a stack list proves you can build, not that you can
design, and the site positions its author as an architect.

Skills reference the projects that prove them and carry no percentage
level: an unmeasured '95%' reads as a template to the technical audience
this portfolio targets.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5 : Articles et internationalisation documentaire

**Files:**

- Create: `src/sanity/schemas/documents/post.ts`
- Modify: `src/sanity/schemas/index.ts`, `sanity.config.ts`, `src/sanity/structure.ts`, `tests/unit/sanity-schemas.test.ts`, `package.json`

**Interfaces:**

- Consumes: la configuration du Studio (tâche 2), les types agrégés (tâches 3 et 4)
- Produces: document `post`, internationalisé **au niveau document** — un document par langue, reliés entre eux. Consommé par les requêtes de la tâche 6.

Contrairement aux documents structurés, un article peut n'exister qu'en une langue et ses textes divergent réellement. Le dupliquer est ici un avantage, pas un coût.

- [ ] **Step 1 : Installer le plugin**

```bash
npm install @sanity/document-internationalization@^6.2.35
```

- [ ] **Step 2 : Compléter le test**

Ajouter à `tests/unit/sanity-schemas.test.ts` :

```ts
describe('articles', () => {
  it('déclare le type post', () => {
    expect(byName('post')).toBeDefined();
  });

  it('porte un champ language, requis par l’internationalisation documentaire', () => {
    const post = byName('post') as {fields: Array<{name: string}>};
    expect(post.fields.map((f) => f.name)).toContain('language');
  });

  it('stocke le corps en Portable Text et non en chaîne', () => {
    const post = byName('post') as {fields: Array<{name: string; type: string}>};
    expect(post.fields.find((f) => f.name === 'body')?.type).toBe('array');
  });
});
```

- [ ] **Step 3 : Lancer pour vérifier l'échec**

Run: `npx vitest run tests/unit/sanity-schemas.test.ts`
Expected: ÉCHEC — `post` introuvable.

- [ ] **Step 4 : Écrire le schéma d'article**

```ts
// src/sanity/schemas/documents/post.ts
import {defineArrayMember, defineField, defineType} from 'sanity';

export const post = defineType({
  name: 'post',
  title: 'Article',
  type: 'document',
  fields: [
    // Rempli par @sanity/document-internationalization ; masqué à l'édition.
    defineField({name: 'language', type: 'string', readOnly: true, hidden: true}),
    defineField({name: 'title', title: 'Titre', type: 'string', validation: (r) => r.required()}),
    defineField({
      name: 'slug',
      title: 'Identifiant d’URL',
      type: 'slug',
      options: {source: 'title', maxLength: 96},
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'summary',
      title: 'Résumé',
      type: 'text',
      rows: 3,
      description: 'Apparaît dans la liste et dans les aperçus de partage.',
    }),
    defineField({
      name: 'publishedAt',
      title: 'Date de publication',
      type: 'datetime',
      validation: (r) => r.required(),
    }),
    defineField({
      name: 'cover',
      title: 'Image de couverture',
      type: 'image',
      options: {hotspot: true},
      fields: [defineField({name: 'alt', title: 'Texte alternatif', type: 'string'})],
    }),
    defineField({
      name: 'tags',
      title: 'Étiquettes',
      type: 'array',
      of: [{type: 'string'}],
      options: {layout: 'tags'},
    }),
    defineField({
      name: 'pinned',
      title: 'Épinglé',
      type: 'boolean',
      initialValue: false,
    }),
    defineField({
      name: 'body',
      title: 'Corps',
      type: 'array',
      of: [
        defineArrayMember({
          type: 'block',
          styles: [
            {title: 'Normal', value: 'normal'},
            {title: 'Titre 2', value: 'h2'},
            {title: 'Titre 3', value: 'h3'},
            {title: 'Citation', value: 'blockquote'},
          ],
          marks: {
            annotations: [
              {
                name: 'link',
                type: 'object',
                title: 'Lien',
                fields: [
                  defineField({
                    name: 'href',
                    type: 'url',
                    validation: (r) => r.uri({scheme: ['http', 'https', 'mailto']}),
                  }),
                ],
              },
            ],
          },
        }),
        defineArrayMember({
          type: 'image',
          options: {hotspot: true},
          fields: [defineField({name: 'alt', title: 'Texte alternatif', type: 'string'})],
        }),
        defineArrayMember({
          type: 'object',
          name: 'codeBlock',
          title: 'Bloc de code',
          fields: [
            defineField({name: 'language', title: 'Langage', type: 'string'}),
            defineField({name: 'code', title: 'Code', type: 'text', rows: 12}),
          ],
          preview: {
            select: {language: 'language', code: 'code'},
            prepare: ({language, code}) => ({
              title: language ?? 'code',
              subtitle: (code ?? '').split('\n')[0],
            }),
          },
        }),
      ],
    }),
  ],
  orderings: [
    {title: 'Plus récent d’abord', name: 'recent', by: [{field: 'publishedAt', direction: 'desc'}]},
  ],
  preview: {
    select: {title: 'title', language: 'language', media: 'cover'},
    prepare: ({title, language, media}) => ({title, subtitle: language, media}),
  },
});
```

- [ ] **Step 5 : Activer l'internationalisation documentaire**

Dans `sanity.config.ts`, ajouter au tableau `plugins` :

```ts
import {documentInternationalization} from '@sanity/document-internationalization';

// … dans plugins: [ … ]
documentInternationalization({
  supportedLanguages: [
    {id: 'fr', title: 'Français'},
    {id: 'en', title: 'English'},
  ],
  schemaTypes: ['post'],
}),
```

Seul `post` est concerné : les autres documents sont internationalisés au niveau champ.

- [ ] **Step 6 : Vérifier**

```bash
npx vitest run tests/unit/sanity-schemas.test.ts
npm run lint && npm run typecheck && npm run build
npx playwright test tests/e2e/studio.spec.ts
```

Expected: 11 tests unitaires PASS, les trois commandes en 0.

- [ ] **Step 7 : Vérifier à l'œil**

Lancer `npm run dev`, ouvrir `/studio`, créer un article en français puis utiliser le sélecteur de langue du plugin pour en créer la traduction anglaise. Confirmer que les deux documents sont reliés et que le champ `language` se remplit seul. Supprimer les deux documents de test. Rapporter ce qui est constaté.

- [ ] **Step 8 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Add posts with document-level internationalisation

Posts use document-level i18n while every other type uses field-level: an
article may exist in one language only and its text genuinely diverges, so
duplicating the document is the point rather than the cost. Structured
documents share their images, stack and dates, which duplication would force
an editor to maintain twice.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6 : TypeGen et requêtes GROQ

**Files:**

- Create: `src/sanity/queries/projects.ts`, `posts.ts`, `profile.ts`, `services.ts`, `index.ts`, `sanity-typegen.json`, `tests/unit/sanity-queries.test.ts`
- Modify: `package.json`, `.gitignore`, `.github/workflows/ci.yml`

**Interfaces:**

- Consumes: tous les schémas (tâches 3 à 5), `sanityFetch` (tâche 2)
- Produces:
  - `sanity.types.ts` — types générés **depuis les requêtes**, pas depuis les schémas seuls
  - requêtes nommées exportées : `PROJECTS_QUERY`, `PROJECT_BY_SLUG_QUERY`, `FEATURED_PROJECTS_QUERY`, `POSTS_QUERY`, `POST_BY_SLUG_QUERY`, `PROFILE_QUERY`, `SERVICES_QUERY` — consommées par le plan 3
  - scripts npm `typegen` et `typegen:check`

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// tests/unit/sanity-queries.test.ts
import {describe, expect, it} from 'vitest';
import * as queries from '@/sanity/queries';

const ALL = Object.entries(queries).filter(([name]) => name.endsWith('_QUERY'));

describe('requêtes GROQ', () => {
  it('exporte les sept requêtes attendues', () => {
    expect(ALL.map(([name]) => name).sort()).toEqual(
      [
        'FEATURED_PROJECTS_QUERY',
        'POSTS_QUERY',
        'POST_BY_SLUG_QUERY',
        'PROFILE_QUERY',
        'PROJECTS_QUERY',
        'PROJECT_BY_SLUG_QUERY',
        'SERVICES_QUERY',
      ].sort(),
    );
  });

  it('filtre les champs internationalisés sur `language`, pas sur `_key`', () => {
    // `_key ==` appartient à la v4 du plugin et renvoie undefined en v5.
    for (const [name, query] of ALL) {
      expect(String(query), `${name} utilise la syntaxe obsolète`).not.toMatch(/\[_key\s*==/);
    }
  });

  it('paramètre la langue au lieu de la coder en dur', () => {
    for (const [name, query] of ALL) {
      const text = String(query);
      if (text.includes('[language ==')) {
        expect(text, `${name} code la langue en dur`).toMatch(/\[language == \$language\]/);
      }
    }
  });

  it('ne renvoie jamais de brouillon dans les requêtes publiques', () => {
    for (const [name, query] of ALL) {
      expect(String(query), `${name} peut renvoyer des brouillons`).not.toMatch(/drafts\./);
    }
  });
});
```

- [ ] **Step 2 : Lancer pour vérifier l'échec**

Run: `npx vitest run tests/unit/sanity-queries.test.ts`
Expected: ÉCHEC — `Cannot find module '@/sanity/queries'`.

- [ ] **Step 3 : Écrire les requêtes**

```ts
// src/sanity/queries/projects.ts
import {defineQuery} from 'next-sanity';

/**
 * `defineQuery` marque la chaîne pour que TypeGen en dérive un type de
 * résultat. Une requête écrite en littéral simple ne serait pas typée.
 */
export const PROJECTS_QUERY = defineQuery(`
  *[_type == "project"] | order(order asc, year desc) {
    _id,
    title,
    "slug": slug.current,
    "summary": summary[language == $language][0].value,
    year,
    client,
    "role": role[language == $language][0].value,
    featured,
    stack,
    "cover": images[0],
    links
  }
`);

export const FEATURED_PROJECTS_QUERY = defineQuery(`
  *[_type == "project" && featured == true] | order(order asc, year desc) [0...4] {
    _id,
    title,
    "slug": slug.current,
    "summary": summary[language == $language][0].value,
    year,
    client,
    stack
  }
`);

export const PROJECT_BY_SLUG_QUERY = defineQuery(`
  *[_type == "project" && slug.current == $slug][0] {
    _id,
    title,
    "slug": slug.current,
    "summary": summary[language == $language][0].value,
    year,
    client,
    "role": role[language == $language][0].value,
    stack,
    images,
    links,
    "context": context[language == $language][0].value,
    "constraint": constraint[language == $language][0].value,
    "decisions": decisions[]{ "text": text[language == $language][0].value },
    outcomes[]{
      value,
      "label": label[language == $language][0].value
    }
  }
`);
```

```ts
// src/sanity/queries/posts.ts
import {defineQuery} from 'next-sanity';

export const POSTS_QUERY = defineQuery(`
  *[_type == "post" && language == $language && defined(publishedAt)]
    | order(pinned desc, publishedAt desc) {
    _id,
    title,
    "slug": slug.current,
    summary,
    publishedAt,
    tags,
    pinned,
    cover
  }
`);

export const POST_BY_SLUG_QUERY = defineQuery(`
  *[_type == "post" && language == $language && slug.current == $slug][0] {
    _id,
    title,
    "slug": slug.current,
    summary,
    publishedAt,
    tags,
    cover,
    body
  }
`);
```

```ts
// src/sanity/queries/profile.ts
import {defineQuery} from 'next-sanity';

export const PROFILE_QUERY = defineQuery(`
  *[_type == "profile"][0] {
    name,
    "role": role[language == $language][0].value,
    location,
    "bio": bio[language == $language][0].value,
    photo,
    available,
    links,
    resumeFr,
    resumeEn
  }
`);
```

```ts
// src/sanity/queries/services.ts
import {defineQuery} from 'next-sanity';

export const SERVICES_QUERY = defineQuery(`
  *[_type == "service"] | order(number asc) {
    _id,
    number,
    stage,
    title,
    "description": description[language == $language][0].value,
    keywords
  }
`);
```

```ts
// src/sanity/queries/index.ts
export * from './projects';
export * from './posts';
export * from './profile';
export * from './services';
```

- [ ] **Step 4 : Relancer le test**

Run: `npx vitest run tests/unit/sanity-queries.test.ts`
Expected: 4 tests PASS.

- [ ] **Step 5 : Configurer et lancer TypeGen**

```json
// sanity-typegen.json
{
  "path": "./src/**/*.{ts,tsx}",
  "schema": "./schema.json",
  "generates": "./sanity.types.ts"
}
```

Ajouter à `package.json` :

```json
"typegen": "sanity schema extract --enforce-required-fields && sanity typegen generate",
"typegen:check": "npm run typegen && git diff --exit-code sanity.types.ts"
```

Ajouter `schema.json` à `.gitignore` — c'est un artefact intermédiaire régénéré à chaque exécution. `sanity.types.ts` est en revanche **commité** : la CI vérifie qu'il est à jour.

```bash
npm run typegen
```

Expected: `sanity.types.ts` créé, contenant un type par requête (`PROJECTS_QUERYResult`, etc.).

- [ ] **Step 6 : Ajouter la vérification à la CI**

Dans `.github/workflows/ci.yml`, insérer après l'étape « Vérification des types » :

```yaml
- name: Types Sanity à jour
  run: npm run typegen:check
```

Ce contrôle fait échouer la CI si un champ a été renommé dans un schéma sans que les types soient régénérés — c'est ce qui transforme un `undefined` silencieux en production en une erreur de build.

- [ ] **Step 7 : Vérifier la chaîne complète**

```bash
npm run lint && npm run typecheck && npm test && npm run build
npm run typegen:check
```

Expected: toutes en 0, et `typegen:check` ne signale aucune différence.

- [ ] **Step 8 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Generate types from the GROQ queries themselves

TypeGen derives the result type of each query, so renaming a schema field
breaks the build instead of yielding undefined in production. The CI check
fails when the committed types drift from the schema.

A unit test rejects the v4 `[_key ==` filter syntax, which returns undefined
against v5 of the internationalized-array plugin without erroring.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7 : Importer le contenu archivé

**Files:**

- Create: `scripts/import-archive.mjs`
- Modify: `package.json`, `.env.example`

**Interfaces:**

- Consumes: `docs/content-archive/*.json` (plan 1, tâche 1), les schémas (tâches 3 à 5)
- Produces: le contenu de l'ancien site présent dans le dataset Sanity

Cet import exige un token **Editor**, distinct du token Viewer de la tâche 1. Il s'exécute une fois.

- [ ] **Step 1 : (humain) Créer un token d'écriture**

Sur `https://sanity.io/manage`, projet → **API** → **Tokens** → **Add API token**, permission **Editor**. L'ajouter à `.env.local` :

```bash
SANITY_API_WRITE_TOKEN=<le token Editor>
```

Ajouter la ligne correspondante à `.env.example`, sans valeur, avec un commentaire indiquant qu'il ne sert qu'à l'import.

- [ ] **Step 2 : Écrire le script d'import**

```js
// scripts/import-archive.mjs
/**
 * Import unique du contenu de l'ancien site vers Sanity.
 *
 * Ce que ce script fait NE PAS migrer, délibérément :
 *   - les 5 articles de blog : génériques, illustrés d'images de banque et
 *     d'une image tierce servie depuis un domaine externe ; la spec §8 les
 *     écarte explicitement ;
 *   - les 4 services de l'ancien site : remplacés par les 5 du
 *     positionnement d'architecte ;
 *   - les niveaux de compétence en pourcentage : remplacés par une
 *     fréquence d'usage.
 *
 * Le script est idempotent : il utilise `createOrReplace` avec des
 * identifiants dérivés des données, donc le relancer ne duplique rien.
 */
import {readFileSync} from 'node:fs';
import {createClient} from '@sanity/client';

const projectId = process.env.NEXT_PUBLIC_SANITY_PROJECT_ID;
const dataset = process.env.NEXT_PUBLIC_SANITY_DATASET;
const token = process.env.SANITY_API_WRITE_TOKEN;

if (!projectId || !dataset || !token) {
  console.error(
    'Variables manquantes. Requis : NEXT_PUBLIC_SANITY_PROJECT_ID, ' +
      'NEXT_PUBLIC_SANITY_DATASET, SANITY_API_WRITE_TOKEN.',
  );
  process.exit(1);
}

const client = createClient({projectId, dataset, token, apiVersion: '2026-09-01', useCdn: false});

const read = (name) =>
  JSON.parse(
    readFileSync(new URL(`../docs/content-archive/${name}.json`, import.meta.url), 'utf8'),
  );

/** Un champ internationalisé du plugin : un tableau d'entrées par langue. */
const i18n = (fr, en) =>
  [
    fr ? {_key: 'fr', _type: 'internationalizedArrayStringValue', value: fr} : null,
    en ? {_key: 'en', _type: 'internationalizedArrayStringValue', value: en} : null,
  ].filter(Boolean);

const i18nText = (fr, en) =>
  [
    fr ? {_key: 'fr', _type: 'internationalizedArrayTextValue', value: fr} : null,
    en ? {_key: 'en', _type: 'internationalizedArrayTextValue', value: en} : null,
  ].filter(Boolean);

const slugify = (s) =>
  s
    .toLowerCase()
    .normalize('NFD')
    .replace(/[̀-ͯ]/g, '')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/(^-|-$)/g, '');

async function importProjects() {
  const projects = read('projects');
  const docs = projects.map((p, index) => ({
    _id: `project-${p.id}`,
    _type: 'project',
    title: p.title,
    slug: {_type: 'slug', current: slugify(p.id)},
    // Les descriptions de l'archive sont en français ; l'anglais reste à écrire.
    summary: i18nText(p.miniDescription || p.description, null),
    year: Number(String(p.duration ?? '').match(/\d{4}/)?.[0]) || undefined,
    client: p.company ?? p.event ?? undefined,
    featured: Boolean(p.featured),
    order: index,
    stack: p.techStack ?? p.tags ?? [],
    links: [
      p.liveUrl && p.liveUrl !== '#'
        ? {_key: 'live', _type: 'link', label: 'Site', url: p.liveUrl}
        : null,
      p.githubUrl && p.githubUrl !== '#'
        ? {_key: 'code', _type: 'link', label: 'Code', url: p.githubUrl}
        : null,
    ].filter(Boolean),
  }));

  for (const doc of docs) await client.createOrReplace(doc);
  return docs.length;
}

async function importExperiences() {
  const experiences = read('experiences');
  const docs = experiences.map((e) => ({
    _id: `experience-${e.id}`,
    _type: 'experience',
    position: i18n(e.position, e.position),
    organisation: e.company,
    location: e.location,
    description: i18nText(null, e.description),
    // `bullet` enveloppe chaque réalisation : Sanity refuse un tableau
    // contenant directement un autre tableau.
    achievements: (e.achievements ?? []).map((a, i) => ({
      _key: `a${i}`,
      _type: 'bullet',
      text: i18n(null, a),
    })),
    technologies: e.technologies ?? [],
  }));

  for (const doc of docs) await client.createOrReplace(doc);
  return docs.length;
}

async function importSkills() {
  const categories = read('skills');
  const map = {
    'Frontend Development': 'frontend',
    'Backend Development': 'backend',
  };
  let count = 0;
  for (const category of categories) {
    for (const item of category.items) {
      await client.createOrReplace({
        _id: `skill-${slugify(item.name)}`,
        _type: 'skill',
        name: item.name,
        category: map[category.category] ?? 'tools',
        // Les pourcentages de l'archive ne mesurent rien : on les traduit en
        // fréquence d'usage, à réviser à la main dans le Studio.
        usage: item.level >= 90 ? 'daily' : item.level >= 75 ? 'regular' : 'familiar',
      });
      count++;
    }
  }
  return count;
}

const results = {
  projects: await importProjects(),
  experiences: await importExperiences(),
  skills: await importSkills(),
};

console.log('Import terminé :');
for (const [name, n] of Object.entries(results)) console.log(`  ${name.padEnd(12)} ${n}`);
console.log('\nNon importés, délibérément : les 5 articles et les 4 services de l’ancien site.');
console.log('À compléter à la main dans le Studio : traductions anglaises, images, champs');
console.log('d’architecture des projets, et les 5 services du positionnement.');
```

Ajouter à `package.json` :

```json
"import:archive": "node --env-file=.env.local scripts/import-archive.mjs"
```

- [ ] **Step 3 : Lancer l'import**

```bash
npm run import:archive
```

Expected: 10 projets, 3 expériences, 34 compétences. Aucun article, aucun service.

- [ ] **Step 4 : Vérifier dans le Studio**

Lancer `npm run dev`, ouvrir `/studio`, et confirmer :

- 10 projets, dont 4 marqués « Mis en avant » ;
- 3 expériences ;
- 34 compétences, aucune avec un pourcentage ;
- aucun article, aucun service.

Vérifier qu'un projet ouvert affiche bien son résumé en français et un onglet anglais vide.

- [ ] **Step 5 : Relancer l'import pour prouver l'idempotence**

```bash
npm run import:archive
```

Rouvrir le Studio et confirmer que les compteurs sont **identiques** — pas de doublons. C'est ce que garantit `createOrReplace` avec des identifiants dérivés.

- [ ] **Step 6 : Vérifier la chaîne**

```bash
npm run lint && npm run typecheck && npm test && npm run build && npm run format:check
```

- [ ] **Step 7 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Import the archived content

Brings across the ten projects, three experiences and thirty-four skills.
Deliberately leaves behind the five blog posts and four services: the spec
drops the former as generic filler illustrated with stock photography, and
replaces the latter with the five services of the architect positioning.

Percentage skill levels become a usage frequency, since an unmeasured 95%
signals a template rather than a level.

The script is idempotent — rerunning it replaces rather than duplicates.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 8 : Prévisualisation des brouillons et revalidation

**Files:**

- Create: `src/app/api/draft-mode/enable/route.ts`, `src/app/api/revalidate/route.ts`, `tests/e2e/draft-mode.spec.ts`
- Modify: `src/app/(site)/[locale]/layout.tsx`, `sanity.config.ts`

**Interfaces:**

- Consumes: `client` et `sanityFetch` (tâche 2), `readToken()` (tâche 1)
- Produces: `/api/draft-mode/enable` protégée par secret, `/api/revalidate` à signature vérifiée, `SanityLive` monté dans le layout du site

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// tests/e2e/draft-mode.spec.ts
import {expect, test} from '@playwright/test';

test('la prévisualisation refuse une requête sans secret', async ({request}) => {
  const response = await request.get('/api/draft-mode/enable', {maxRedirects: 0});
  expect([401, 400]).toContain(response.status());
});

test('la prévisualisation refuse un secret erroné', async ({request}) => {
  const response = await request.get('/api/draft-mode/enable?secret=faux&slug=/fr', {
    maxRedirects: 0,
  });
  expect([401, 400]).toContain(response.status());
});

test('le webhook de revalidation refuse une signature absente', async ({request}) => {
  const response = await request.post('/api/revalidate', {
    data: {_type: 'project'},
  });
  expect(response.status()).toBe(401);
});
```

- [ ] **Step 2 : Lancer pour vérifier l'échec**

Run: `npx playwright test tests/e2e/draft-mode.spec.ts`
Expected: ÉCHEC — les routes renvoient 404.

- [ ] **Step 3 : Vérifier que la vérification de signature est disponible**

`parseBody` s'importe depuis `next-sanity/webhook`, un sous-chemin de `next-sanity` déjà installé. **N'installe pas `@sanity/webhook` séparément** : le projet interdit toute dépendance non importée directement, et ce paquet arrive comme dépendance transitive.

Confirme que le sous-chemin existe avant d'écrire la route :

```bash
node -e "import('next-sanity/webhook').then(m => console.log(Object.keys(m)))"
```

Expected: la liste inclut `parseBody`. Si ce n'est pas le cas, signale-le plutôt que d'improviser une vérification de signature maison.

- [ ] **Step 4 : Écrire la route de prévisualisation**

```ts
// src/app/api/draft-mode/enable/route.ts
import {draftMode} from 'next/headers';
import {redirect} from 'next/navigation';
import {NextRequest} from 'next/server';

/**
 * Active le mode brouillon de Next pour prévisualiser du contenu non publié.
 * Protégée par un secret : sans lui, n'importe qui pourrait lire les
 * brouillons du Studio.
 */
export async function GET(request: NextRequest) {
  const {searchParams} = new URL(request.url);
  const secret = searchParams.get('secret');
  const slug = searchParams.get('slug') ?? '/';

  if (!process.env.SANITY_REVALIDATE_SECRET) {
    return new Response('Prévisualisation non configurée', {status: 500});
  }

  if (secret !== process.env.SANITY_REVALIDATE_SECRET) {
    return new Response('Secret invalide', {status: 401});
  }

  // Ne rediriger que vers un chemin interne : une URL absolue permettrait
  // une redirection ouverte.
  if (!slug.startsWith('/')) {
    return new Response('Chemin invalide', {status: 400});
  }

  (await draftMode()).enable();
  redirect(slug);
}
```

- [ ] **Step 5 : Écrire le webhook de revalidation**

```ts
// src/app/api/revalidate/route.ts
import {parseBody} from 'next-sanity/webhook';
import {revalidateTag} from 'next/cache';
import {NextRequest, NextResponse} from 'next/server';

/**
 * Webhook appelé par Sanity à chaque publication. La signature est vérifiée :
 * sans elle, n'importe qui pourrait forcer une régénération du site.
 */
export async function POST(request: NextRequest) {
  const secret = process.env.SANITY_REVALIDATE_SECRET;
  if (!secret) {
    return new NextResponse('Webhook non configuré', {status: 500});
  }

  const {isValidSignature, body} = await parseBody<{_type?: string}>(request, secret);

  if (!isValidSignature) {
    return new NextResponse('Signature invalide', {status: 401});
  }

  if (!body?._type) {
    return new NextResponse('Corps sans _type', {status: 400});
  }

  revalidateTag(body._type);
  return NextResponse.json({revalidated: true, type: body._type});
}
```

- [ ] **Step 6 : Monter `SanityLive` dans le layout du site**

Dans `src/app/(site)/[locale]/layout.tsx`, à l'intérieur de `<body>`, après le pied de page :

```tsx
import {SanityLive} from '@/sanity/lib/live';
import {VisualEditing} from 'next-sanity/visual-editing';
import {draftMode} from 'next/headers';

// … dans le composant, avant le return :
const {isEnabled: isDraft} = await draftMode();

// … dans le JSX, après <SiteFooter /> :
<SanityLive includeDrafts={isDraft} />;
{
  isDraft && <VisualEditing />;
}
```

- [ ] **Step 7 : Vérifier**

```bash
npm run lint && npm run typecheck && npm run build
npx playwright test tests/e2e/draft-mode.spec.ts
npm run test:e2e
```

Expected: les trois commandes en 0, les 3 tests de prévisualisation PASS, et **toute la suite existante toujours verte** — en particulier `/fr` et `/en` doivent rester marquées `●` (statiques) au build.

Si le montage de `SanityLive` rend les pages dynamiques, c'est un problème réel : le signaler plutôt que d'accepter la régression.

- [ ] **Step 8 : (humain) Configurer le webhook côté Sanity**

Sur `https://sanity.io/manage`, projet → **API** → **Webhooks** → **Create webhook** :

- URL : `https://<domaine de production>/api/revalidate`
- Dataset : `production`
- Trigger on : Create, Update, Delete
- Secret : la valeur de `SANITY_REVALIDATE_SECRET`

- [ ] **Step 9 : Commit**

```bash
git add -A
git commit -m "feat(sanity): Add secret-protected draft preview and signed revalidation

Both endpoints refuse unauthenticated calls: without the secret anyone could
read Studio drafts, and without signature verification anyone could force
the site to regenerate.

The preview route only redirects to internal paths, so it cannot be turned
into an open redirect.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Auto-relecture

**Couverture de la spec.** §7 modèle de contenu → tâches 3 à 5 (tous les types du tableau sont présents : `project`, `post`, `service`, `experience`, `skill`, `profile`, `settings`) · §7 i18n hybride → tâche 3 (niveau champ) et tâche 5 (niveau document) · §7 typage → tâche 6 · §8 migration → tâche 7, avec les exclusions explicites de la spec · §9 sécurité, token serveur et route de brouillon protégée → tâches 1 et 8 · §4.2 arborescence `src/sanity/` → tâches 2 à 6.

**Volontairement hors de ce plan.** Le rendu des pages (accueil, expertise, travaux, parcours, écrits, contact), shadcn/ui, le formulaire Resend, le Portable Text rendu → plan 3. Images OG, JSON-LD, `generateMetadata` par page, budget de performance, axe-core, sitemap dynamique → plan 4. **Le CSP**, reporté du plan 1 : il doit atterrir au plan 3, avant que le formulaire de contact n'introduise une entrée utilisateur.

**Points de vigilance pour l'exécutant.**

1. La tâche 1 exige un humain. Ne pas tenter de créer un projet Sanity par un agent.
2. Le filtre GROQ est `champ[language == $language][0].value`. La forme `_key ==` est la v4 du plugin : elle ne lève aucune erreur et renvoie `undefined`.
3. `@sanity/language-filter` est un peer **requis** de `sanity-plugin-internationalized-array`, pas optionnel.
   3bis. Sanity refuse un tableau contenant directement un autre tableau. Comme `internationalizedArrayString` **est** un tableau, toute liste de textes traduits passe par l'objet `bullet`.
4. Le Studio a sa propre racine HTML dans `src/app/(studio)/`. Ne jamais créer `src/app/layout.tsx` : cela casserait les deux racines et le `lang` par langue du site.
5. `sanity.types.ts` est commité et vérifié par la CI ; `schema.json` est un artefact ignoré.
6. Après le montage de `SanityLive` (tâche 8), vérifier que `/fr` et `/en` restent statiques. Une bascule en rendu dynamique est une régression du budget de performance.
